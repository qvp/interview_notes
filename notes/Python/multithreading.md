# Threads

## Что такое многозадачность
**Конкурентность** - говоря, что две задачи выполняются конкурентно, мы имеем в виду, что они работают в одно и то же время. Пусть работают два приложения. В конкурентной системе мы можем переключаться между ними, дав немного поработать сначала одному, а потом другому. Если делать это достаточно быстро, создается впечатление, что два дела делаются одновременно.

**Параллелизм** - хотя конкурентность подразумевает, что несколько задач выполняется одновременно, это еще не значит, что они работают параллельно. Говоря о параллельной работе, мы имеем в виду, что две задачи или более не просто чередуются, а выполняются строго в одно и то же время.

**Вытесняющая многозадачность** - вид многозадачности, в котором операционная система сама передает управление от одной выполняемой программы другой в случае завершения операций ввода-вывода, возникновения событий в аппаратуре компьютера, истечения таймеров и квантов времени, или же поступлений тех или иных сигналов от одной программы к другой. В этом виде многозадачности процессор может быть переключен с исполнения одной программы на исполнение другой без всякого пожелания первой программы и буквально между любыми двумя инструкциями в её коде. Когда операционная система переключает задачи, мы говорим, что имеет место вытеснение. Обычно для этого используется либо несколько потоков, либо несколько процессов.

**Кооперативная (Совместная) многозадачность** - в этой модели мы не полагаемся для переключения между задачами на операционную систему, а явно определяем в коде приложения точки, где можно уступить управление другой задаче. Исполняемые задачи кооперируются, т. е. говорят приложению: "Я сейчас на время приостановлюсь, а ты можешь пока выполнять другие задачи".

## Что такое Поток
Потоки можно представлять себе как облегченные процессы. Кроме того, это наименьшая единица выполнения, которая может управляться операционной системой. У потоков нет своей памяти, они пользуются памятью создавшего их процесса. Потоки ассоциированы с процессом, создавшим их. С каждым процессом всегда ассоциирован по меньшей мере один поток, обычно называемый главным.

Процесс может создавать дополнительные потоки, которые называются рабочими или фоновыми. Эти потоки могут конкурентно выполнять другую работу наряду с главным потоком. Потоки, как и процессы, могут работать параллельно на многоядерном процессе, и операционная система может переключаться между ними с помощью квантования времени. Обычное Python-приложение создает процесс и главный поток, который отвечает за его выполнение.

Python thread = OS thread + Python thread state.

## Многопоточность
Это несколько конкурентно выполняющихся потоков в рамках одного процесса (с общей памятью). То есть потоки могут выполняться конкурентно но не параллельно.

Многопоточность полезна только для задач, ограниченных производительностью ввода-вывода, потому что нам мешает глобальная блокировка интерпретатора. То есть потоки подходят для случаев, когда преобладает I/O а CPU значительную часть времени простаивает в ожидании, но код при этом должен оставаться синхронным.

## GIL
GIL (Global Interpreter Lock – глобальная блокировка интерпретатора) - это способ синхронизации потоков, при котором, в рамках одного процесса только один поток может находится в состоянии выполнения. Его задача состоит в том, чтобы сделать интерпретатор CPython потокобезопасным. Однопоточные программы работают быстрее (нет мьютексов в структурах данных). Простая интеграция библиотек на C, которые обычно тоже не потокобезопасны.

Чтобы получить GIL, поток сначала проверяет, захвачен ли GIL каким-либо другим потоком. Если это не так, поток немедленно получает GIL. В противном случае он ждет, пока GIL не будет освобожден. Он ожидает фиксированного интервала времени, называемого интервалом переключения (по умолчанию 5 мс), и если GIL не будет выпущен в течение этого времени, он устанавливает флаги eval_breaker и gil_drop_request. Флаг eval_breaker указывает потоку, удерживающему GIL, приостановить выполнение байт-кода, а gil_drop_request объясняет, почему.

Поток, удерживающий GIL, видит флаги, когда при запуске следующей итерации цикла, и освобождает GIL. Он уведомляет об этом потоки, ожидающие GIL, и один из них захватывает GIL. Какой именно поток получит GIL - зависит от операционной системы, так что это может быть поток, который установил флаги, а может быть и другой поток, также ожидающий GIL.

Поток может добровольно освободить GIL до того, как другой поток установит eval_breaker и gil_drop_request. В общем случае поток может удерживать GIL только во время работы с объектами Python. Таким образом, CPython применяет шаблон release-perform-acquire не только к операциям ввода-вывода, но и к другим блокирующим вызовам в ОС, таким как select() и pthread_mutex_lock (), а также к тяжелым вычислениям в чистом C. Например, хэш-функции в стандартном модуле hashlib освобождают GIL. Это позволяет нам ускорить код Python, который вызывает такие функции, используя многопоточность.

Эффект конвоя происходит потому, что каждый раз, когда поток, связанный с вводом-выводом, выполняет операцию ввода-вывода, он освобождает GIL, и когда он пытается повторно получить GIL, то GIL, скорее всего, уже будет занят потоком, связанным с процессором. Таким образом, поток, связанный с вводом-выводом, должен подождать не менее 5 мс, прежде чем он сможет установить eval_breaker и gil_drop_request и заставить поток, связанный с процессором, освободить GIL.

Поток, который принуждают освободить GIL, ждёт до того момента, пока другой поток не захватит блокировку. В результате поток, привязанный к подсистеме ввода/вывода, захватывает GIL после одного интервала переключения. Если бы этого механизма не существовало, последствия эффекта сопровождения были бы ещё хуже.

С помощью sys.setswitchinterval(interval) можно изменить время переключения между потоками. Меньшие интервалы переключения делают потоки, связанные с вводом-выводом, более отзывчивыми. Но слишком малые интервалы переключения приводят к большим накладным расходам, вызванным большим количеством переключений контекста. Интервал настраивают в зависимости от того сколько занимают операции ввода\вывода.

GIL делает следующее потокобезопасным:
- подсчет ссылок
- изменяемые структуры данных
- глобальные и общесистемные данные
- Расширения C

Почему трудно удалить GIL:
- Сборка мусора на основе подсчета ссылок не подходит для многопоточности. Единственным решением является реализация трассирующего сборщика мусора, который реализуют JVM, CLR, Go и другие среды выполнения без GIL.
- Удаление GIL затрагивает существующие расширения на C. Нет никакого способа обойти это.  

https://habr.com/ru/articles/592189/  
https://docs.python.org/3/c-api/init.html#releasing-the-gil-from-extension-code  

Начиная с версии 3.14 появилась альтернативная версия интерпретатора CPython без GIL.    
Внедрена инкрементальная сборка мусора (Incremental GC). Вместо трех поколений объектов теперь используются два: молодое и старшее. Старшее поколение делится на проверенную и ожидающую части, что позволяет выполнять сборку небольшими порциями. Снизить длительность и частоту пауз (“stop-the-world”), которые возникали при полной сборке мусора в многопоточной среде без GIL.

## Обработка исключений в потоках
Напрямую перехватить исключение нельзя, но можно положить его в переменную.

## Чем отличается поток от процесса
С помощью процесса можно организовать параллельное выполнение, а с помощью потока нет. Процессы изолированны и имеют свои ресурсы, а потоки нет. Процессы более дорогостоящие.

## Какие данные являются thread-safe
GIL лиш помогает интерпретатору быть потокобезопасным, но за код программы должен отвечать разработчик. Потокобезопасность осуществляется с помощью замков, семафоров.

## Пространство имен внутри потока
Потоку доступны все ресурсы процесса.

## Thread-Local Data
Локальные данные потока - это данные, значения которых зависят от потока. Чтобы управлять локальными данными потока, просто создайте экземпляр threading.local() (или подкласс) и сохраните в нем атрибуты.

Это обеспечивает полезную альтернативу необходимости передавать локальный экземпляр потока каждой функции, выполняемой потоком, которому требуется доступ к локальным данным потока.

Альтернативой локальному хранению данных потока является расширение класса threading.Thread и хранение данных, специфичных для потока, в виде переменных экземпляра.

Можно использовать как global thread local или static thread local в качестве атрибута класса.

## Что такое Race Conditions, какие есть способы его избежать
Состояние гонки - это ошибка программирования многопоточной системы, при которой работа системы зависит от того, в каком порядке выполняются части кода. Вознимает когда несколько потоков ведут запись одних и тех же данных. Чтобы избежать конкуренции за ресурсы нужно синхронизировать запись с помощь замков (Lock) или семафоров.

## Lock
Самый простой (базовый) приметив, он не принадлежит конкретному потоку. Любой поток, попытавшийся захватить такую блокировку будет заблокирован до того момента как ее получит (даже если этот поток уже ей владеет). Имеет два состояния: закрыто/открыто.

## RLock
Реентерабельная (c повторным входом) блокировка. Блокирует поток только в том случае, если блокировка захвачены другим потоком. В основном используется для вложенного доступа к ресурсам.

## Event
Это один из самых простых механизмов взаимодействия между потоками: один поток сигнализирует о событии, а другие ожидают его. И пока они ожидают это событие - они заблокированы.

## Condition
Более продвинутый вариант события, основанный на Lock.

## Semaphore
Семафоры - более сложный механизм блокировок. Внутри семафора - счетчик, в отличии от Lock, в которой просто флажок. Семафор блокирует поток только когда более заданного числа потоков пытаются захватить семафор. Обычно используется, чтобы лимитировать доступ к ресурсу.

## Что такое deadlock, когда происходит и как его избежать
Это взаимная блокировка нескольких потоков. Чаще всего он случается, когда поток уже заблокировал ресурс А, после чего пытается провести блокировку Б, другой же поток заблокировал ресурс Б, после чего пытается заблокировать ресурс А.
??? timeout + retry ??? Postgres может автоматически проверить взаимную блокировку по истечении времени указанном в deadlock_timeout (по умолчанию 1 секунда).

## Стек потока\стек вызовов ???

## Context switch ???
https://en.wikipedia.org/wiki/Context_switch

## Context Vars
Переменные контекста помогают организовать локальное хранилище внутри потока\корутины\генератора. Они работают единообразно при любых переключениях контекста:
- Разные потоки.
- Цепочки вызовов асинхронных функций.
- Создание новых задач на event loop (ensure_future / create_task).
- Создание генераторов.

**Важно:** переменные контекста следует создавать на верхнем уровне модуля, а не в замыканиях.  
**Важно 2:** проблема с генераторами - их могут возобновить в любом месте - непонятен их контекст.  

https://habr.com/ru/company/domclick/blog/521702/  

## concurrent.futures ???

## Thread daemon ???
